# HOMR OMR RunPod Serverless — Dependencies
# AGPL-3.0 licensed service

# Core OMR engine
homr>=0.6.2

# NOTE: onnxruntime-gpu is installed separately in the Dockerfile
# with --no-deps to prevent conflict with the CPU onnxruntime package.
# Do NOT add onnxruntime or onnxruntime-gpu here.

# RunPod serverless SDK
runpod>=1.7.0

# Image handling
Pillow>=10.0.0
pillow-heif>=0.10.0

# Clef classifier (SVM model inference)
scikit-learn>=1.3.0
