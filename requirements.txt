# HOMR OMR RunPod Serverless — Dependencies
# AGPL-3.0 licensed service

# Core OMR engine
homr>=0.6.2

# GPU inference (replaces CPU onnxruntime that homr pulls in)
onnxruntime-gpu>=1.17.0

# RunPod serverless SDK
runpod>=1.7.0

# Image handling (likely already pulled by homr, but pin for clarity)
Pillow>=10.0.0
