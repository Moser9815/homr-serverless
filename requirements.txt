# HOMR OMR RunPod Serverless — Dependencies
# AGPL-3.0 licensed service

# Core OMR engine
homr>=0.6.2

# GPU inference — replaces the CPU onnxruntime that homr pulls in.
# onnxruntime-gpu bundles its own CUDA/cuDNN libs, so no CUDA base image needed.
onnxruntime-gpu>=1.17.0

# RunPod serverless SDK
runpod>=1.7.0

# Image handling
Pillow>=10.0.0
